Whole Numbers="Create a mixed quiz for CBSE Class 6 Mathematics on the topic 'Whole Numbers'. Include multiple-choice questions (MCQs), short-answer questions, true/false statements, and optionally 2–3 match-the-following items. Use clear, age-appropriate language.

Cover these sub-topics and key terms:
- Basics & Number System: definition of whole numbers; relation to natural numbers; the set of whole numbers (0,1,2,3,…)
- Place Value & Representation: place-value charts; standard/word/expanded form; rounding, comparing, ordering; >, <, = ; number-line representation
- Operations on Whole Numbers: addition, subtraction (with regrouping), multiplication, division (long division, remainders)
- Special Number Properties: even/odd; prime/composite; factors/multiples; GCF (HCF) and LCM
- Patterns & Problem Solving: number patterns; estimation; mental math; word problems

Question mix & difficulty targets:
- ~10 MCQs (exactly 4 meaningful options; one correct) 
- ~5 short-answer questions (2–5 acceptable answers each) 
- ~5 true/false questions
- Difficulty distribution overall: simple ~40%, moderate ~40%, tough ~20%

Quality rules:
- MCQ choices must be real phrases (no 'A/B/C/D', 'Option 1/2/3/4', 'text', 'none', 'all of the above', or 1–3 letter strings). 
- Avoid ambiguous/trick wording; keep numbers within realistic Class 6 ranges.
- Provide a brief explanation for every question.
- If you include any image-based MCQ, add a public 'image_url' for the image.

Output format (JSON ONLY):
Return only valid JSON with:
{
  'topic': 'CBSE Class 6 Mathematics | Whole Numbers',
  'questions': [
    {
      'id': 'string',
      'type': 'mcq',
      'prompt': 'string (>=25 chars)',
      'difficulty': 'simple|moderate|tough',
      'choices': [{'text':'...'},{'text':'...'},{'text':'...'},{'text':'...'}],
      'correct_index': 0,
      'explanation': 'string',
      'image_url': 'optional string'
    },
    {
      'id': 'string',
      'type': 'match',
      'prompt': 'string',
      'difficulty': 'simple|moderate|tough',
      'match_left': ['...','...'],
      'match_right': ['...','...'],
      'match_answer': [0,1],
      'explanation': 'string'
    },
    {
      'id': 'string',
      'type': 'short',
      'prompt': 'string',
      'difficulty': 'simple|moderate|tough',
      'expected_answers': ['ans1','ans2'],
      'explanation': 'string'
    },
    {
      'id': 'string',
      'type': 'tf',
      'prompt': 'string',
      'difficulty': 'simple|moderate|tough',
      'correct_bool': true,
      'explanation': 'string'
    }
  ]
}


